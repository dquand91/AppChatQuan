<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="luongduongquan.com.chatapp.ChatMessageActivity">

    <ImageButton
        android:id="@+id/btn_emoji_message"
        android:background="@null"
        android:src="@drawable/ic_emoticon"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:padding="4dp"
        android:layout_width="40dp"
        android:layout_height="40dp"/>

    <ImageButton
        android:id="@+id/btn_send_message"
        android:background="@null"
        android:src="@android:drawable/ic_menu_send"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:padding="4dp"
        android:layout_width="40dp"
        android:layout_height="40dp"/>

    <EditText
        android:id="@+id/edt_content_input_message"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@id/btn_send_message"
        android:layout_toRightOf="@id/btn_emoji_message"
        android:hint="Enter your message..."
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    
    <ListView
        android:id="@+id/list_message_chat_room"
        android:layout_above="@+id/edt_content_input_message"
        android:layout_alignParentTop="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:dividerHeight="16dp"
        android:layout_marginBottom="16dp"


        ></ListView>


</RelativeLayout>
